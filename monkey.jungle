# Set the manifest file
project.manifest = manifest.xml

# Explicitly include the settings subfolder if it's not being picked up
base.sourcePath = source
base.resourcePath = resources;resources/strings;resources;resources/properties;resources/settings
